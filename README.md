# DiceCraft Live Content

Public, data-only runtime content for DiceCraft. No executable Swift code is distributed here.

The private game source remains in `ZimaBlueAM/DiceCraft`. This repository intentionally contains only validated JSON manifests/configuration and public runtime assets.
